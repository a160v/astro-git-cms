{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: dialog for picking the project folder; everything else goes through this app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
